@if(filled($settings->promo_title))
    <section class="mx-auto w-full max-w-7xl px-4 py-16 sm:px-6 lg:px-8"><div class="store-primary-bg space-y-5 p-12 text-center text-white">@if(filled($settings->promo_eyebrow))<p class="text-[10px] font-bold uppercase tracking-widest text-white/70">{{ $settings->promo_eyebrow }}</p>@endif<h2 class="text-3xl font-black uppercase tracking-tight">{{ $settings->promo_title }}</h2>@if(filled($settings->promo_description))<p class="mx-auto max-w-2xl text-sm leading-relaxed text-white/85">{{ $settings->promo_description }}</p>@endif@if(filled($settings->promo_button_label) && filled($settings->promo_button_url))<a href="{{ $settings->promo_button_url }}" class="inline-block bg-white px-8 py-4 text-xs font-bold uppercase tracking-widest store-primary-text hover:opacity-90">{{ $settings->promo_button_label }}</a>@endif</div></section>
@endif
